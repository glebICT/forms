alert("Hi there!")
// document.addEventListener('DOMContentLoaded', function () {
//     const submitButton = document.querySelector('button[type="submit"]');
//     const form = document.querySelector('form');

//     form.addEventListener('mousemove', function (event) {
//         const rect = submitButton.getBoundingClientRect();
//         const distance = Math.sqrt(
//             Math.pow(event.clientX - (rect.left + rect.width / 2), 2) +
//             Math.pow(event.clientY - (rect.top + rect.height / 2), 2)
//         );

//         if (distance < 100) {
//             const formRect = form.getBoundingClientRect();
//             const newLeft = Math.random() * (formRect.width - rect.width);
//             const newTop = Math.random() * (formRect.height - rect.height);

//             submitButton.style.position = 'absolute';
//             submitButton.style.left = `${newLeft}px`;
//             submitButton.style.top = `${newTop}px`;
//         }
//     });
// });

